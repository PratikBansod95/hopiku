{
  "name": "hopiku",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:playable": "npm run build && node scripts/package-playable.mjs",
    "generate:sfx": "node scripts/generate-sfx.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
